## 1. 관리자가 예약을 생성하는 경우
지난 미션 요구사항을 검토하고, 이 스터디는 Spring에 대한 스터디인 점을 감안하여, `ADMIN` 역할의 멤버가 이름을 기반으로 하여 예약을 생성하는 것은 API 명세라고 판단, 그대로 유지하기로 했습니다.

reservation은 JPA로 마이그레이션하게 되어 member, time, theme을 객체로 갖게 되며, 예약을 생성하는 트랜잭션 범위 안에 각 객체들을 모두 쿼리 조회하여 영속성 컨텍스트에 등록하는 작업을 해야 했습니다.

이 때, 기존 member 테이블의 name은 중복 가능한 필드였으며, 관리자가 입력한 이름이 중복된 이름인 경우에는 `findByName` 쿼리로 여러 레코드가 반환되며, 이 중에서 어느 레코드를 선택해야 하는지 모호해진다는 문제점이 있었습니다.

따라서, member.name 필드를 member.nickname 필드로 마이그레이션했으며, nickname 필드는 유니크 제약 조건을 등록하여 오직 한 개의 레코드만 반환하게 하였습니다.

## 2. soft-delete -> hard-delete
기존의 JDBC 스타일에서는 각 레코드를 soft-delete하는 방식으로 구성되어 있었습니다. 하지만 제가 생각하기에는 soft-delete 방식을 사용하기 위해서는 테이블 종속적인 TTL까지 고려해야 한다는 생각이 들었습니다. 이는 개발 볼륨에 비해 얻는 이득이 적다고 판단되어 hard-delete하는 방식으로 수정하게 되었습니다.
